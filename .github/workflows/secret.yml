name: Use GitHub Secret

on:
  push:
    branches: ["main"]

jobs:
  secret-test:
    runs-on: ubuntu-latest

    steps:
      - name: Use API Key
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          echo "API key is securely available to the workflow"
          echo "Secret has been loaded successfully"